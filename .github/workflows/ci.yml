name: Backend CI

on:
  push:
    branches:
      - main
      - agent/refine-prompts-vision-20260805
  pull_request:
    branches:
      - main

permissions:
  contents: write

jobs:
  patch-test-commit:
    if: github.head_ref == 'agent/persist-vision-and-length-20260805' && github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/persist-vision-and-length-20260805
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Apply server patch
        run: python3 scripts/applyPersistVisionLength.py

      - name: Install dependencies
        run: npm ci --ignore-scripts

      - name: Run syntax checks
        run: npm run check

      - name: Run full backend tests
        run: npm test

      - name: Commit tested implementation
        shell: bash
        run: |
          cat > .github/workflows/ci.yml <<'YAML'
          name: Backend CI

          on:
            push:
              branches:
                - main
                - agent/refine-prompts-vision-20260805
            pull_request:
              branches:
                - main

          jobs:
            test:
              runs-on: ubuntu-latest
              steps:
                - uses: actions/checkout@v4
                - uses: actions/setup-node@v4
                  with:
                    node-version: 22
                    cache: npm
                - run: npm ci --ignore-scripts
                - run: npm run check
                - run: npm test
          YAML
          rm .github/workflows/one-off-persist-vision-length.yml scripts/applyPersistVisionLength.py
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add server.js promptRules.js attachmentContext.js package.json tests .github/workflows/ci.yml .github/workflows/one-off-persist-vision-length.yml scripts/applyPersistVisionLength.py
          git commit -m "Persist verified image context and honor reply floors"
          git push origin HEAD:agent/persist-vision-and-length-20260805

  test:
    if: github.head_ref != 'agent/persist-vision-and-length-20260805'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci --ignore-scripts
      - run: npm run check
      - run: npm test
